# خطوات تشغيل تطبيق "دفتر المستفيدين" برا كلود

اتبعي الخطوات بالترتيب. كل خطوة بسيطة ونسخ ولصق بس.

---

## الخطوة 1: إنشاء قاعدة البيانات (Firebase) — مجانية

1. روحي إلى: https://console.firebase.google.com
2. سجلي دخول بحساب جوجل (جيميل)
3. اضغطي **"Add project"** (إضافة مشروع)
4. اكتبي اسم أي اسم (مثلاً: daftar) واضغطي التالي حتى ينتهي الإنشاء
5. بعد إنشاء المشروع، من القائمة اليسار اضغطي **"Build" > "Firestore Database"**
6. اضغطي **"Create database"**، اختاري أي موقع قريب (مثل: eur3 أو me-central)، واختاري **"Start in test mode"**
7. رجعي لصفحة المشروع الرئيسية (Project Overview)، اضغطي على أيقونة **</> (Web)** لإضافة تطبيق ويب
8. سموه أي اسم واضغطي **"Register app"**
9. بيطلع لك كود فيه قيم زي:
   ```
   apiKey: "AIza...",
   authDomain: "daftar-xxxx.firebaseapp.com",
   projectId: "daftar-xxxx",
   ...
   ```
10. افتحي ملف `src/firebase.js` بالمشروع، واستبدلي كل `PASTE_...` بالقيمة المقابلة لها

---

## الخطوة 2: نشر التطبيق (Netlify) — مجاني، بدون برمجة

1. روحي إلى: https://app.netlify.com/drop
2. لازم أول تجهزين ملفات البناء. إذا ما عندك خبرة تقنية، أسهل طريقة:
   - حملي **Node.js** من https://nodejs.org (اختاري النسخة LTS)
   - افتحي برنامج **Terminal** (أو Command Prompt بويندوز) جوا مجلد المشروع
   - نفذي الأوامر التالية بالترتيب:
     ```
     npm install
     npm run build
     ```
   - بينتج مجلد اسمه `dist`
3. اسحبي مجلد `dist` وفلتيه فوق صفحة netlify.com/drop
4. خلال ثوان بيعطيك رابط مباشر (مثل: `daftar-1234.netlify.app`) — هذا رابطك المستقل بدون أي أثر لكلود

---

## الخطوة 3: تثبيته كأيقونة على الجوال

1. افتحي الرابط الجديد من Netlify بمتصفح الجوال (Safari أو Chrome)
2. من قائمة المتصفح اختاري **"إضافة إلى الشاشة الرئيسية"**
3. خلاص، صار عندك أيقونة تفتح التطبيق مباشرة بدون أي شريط كلود

---

## ملاحظة مهمة

بما إن قاعدة البيانات (Firestore) مفتوحة بوضع "test mode"، أي شخص يعرف رابط قاعدة البيانات نظريًا يقدر يوصلها. لأنك تستخدمينها لبياناتك الخاصة بس ومو تطبيق عام للجمهور، هذا مقبول. إذا حبيتي حماية إضافية لاحقًا، قوليلي وأوضح لك كيف تفعّلين "Firestore Security Rules".
